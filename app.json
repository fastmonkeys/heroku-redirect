{
  "name": "heroku-redirect",
  "stack": "heroku-20",
  "env": {
    "LOCATION": {
      "description": "The location where all incoming requests to this app are redirected to, for example \"https://www.example.com\".",
      "required": true
    }
  },
  "buildpacks": [
    { "url": "https://github.com/heroku/heroku-buildpack-nginx.git" }
  ]
}
